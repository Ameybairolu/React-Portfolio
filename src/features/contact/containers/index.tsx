import { ReactElement } from "react";
import Contact from "../components/contact";

const ContactContainer = (): ReactElement<any, any> => {
    return <Contact />;
};

export default ContactContainer;
