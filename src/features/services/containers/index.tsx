import { ReactElement } from "react";
import Services from "../components/services";

const ServicesContainer = (): ReactElement<any, any> => {
    return <Services />;
};

export default ServicesContainer;
