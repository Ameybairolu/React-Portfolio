import { ReactElement } from "react";
import Portfolio from "../components/portfolio";

const PortfolioContainer = (): ReactElement<any, any> => {
    return <Portfolio />;
};

export default PortfolioContainer;
